<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="dkgroup_custumizations.list">
      <field name="name">dkgroup_custumizations list</field>
      <field name="model">dkgroup_custumizations.dkgroup_custumizations</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="dkgroup_custumizations.action_window">
      <field name="name">dkgroup_custumizations window</field>
      <field name="res_model">dkgroup_custumizations.dkgroup_custumizations</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="dkgroup_custumizations.action_server">
      <field name="name">dkgroup_custumizations server</field>
      <field name="model_id" ref="model_dkgroup_custumizations_dkgroup_custumizations"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="dkgroup_custumizations" id="dkgroup_custumizations.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="dkgroup_custumizations.menu_1" parent="dkgroup_custumizations.menu_root"/>
    <menuitem name="Menu 2" id="dkgroup_custumizations.menu_2" parent="dkgroup_custumizations.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="dkgroup_custumizations.menu_1_list" parent="dkgroup_custumizations.menu_1"
              action="dkgroup_custumizations.action_window"/>
    <menuitem name="Server to list" id="dkgroup_custumizations" parent="dkgroup_custumizations.menu_2"
              action="dkgroup_custumizations.action_server"/>
    -->
  </data>
</odoo>